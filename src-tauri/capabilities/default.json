{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-capability",
  "description": "Default capabilities for FinalCall windows",
  "windows": ["main", "reminder-overlay-*"],
  "permissions": [
    "core:default",
    "positioner:default",
    "allow-commands"
  ]
}
